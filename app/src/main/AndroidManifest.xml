<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahrous.movies">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/popcorn"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/popcorn"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.mahrous.movies.Test"></activity>
        <activity android:name="com.example.mahrous.movies.MainActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name="com.example.mahrous.movies.moviedetails.MovieDetails"
            android:theme="@style/Detail" />
        <activity android:name="com.example.mahrous.movies.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.mahrous.movies.kotlin.KotlinActivity" />
        <activity
            android:name="com.example.mahrous.movies.tv_details.TvDetails"
            android:theme="@style/Detail" />
    </application>

</manifest>